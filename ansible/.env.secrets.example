# Example secrets file for Ansible
# Copy this to .env.secrets and fill in your actual values
# This file is gitignored - never commit .env.secrets to git

# Password for raolivei@node-x (set via Raspberry Pi Imager)
# Only needed if SSH keys aren't working
env_target_password='your-password-here'









