apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: bind-tsig-secret
  namespace: bind
spec:
  refreshInterval: 24h
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: bind-tsig-secret
    creationPolicy: Owner
  data:
    - secretKey: tsig-secret
      remoteRef:
        key: secret/pi-fleet/external-dns/tsig-secret
        property: secret
