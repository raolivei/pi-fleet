apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cert-manager # TLS certificate management
  - issuers # ClusterIssuer (self-signed)
  - vault # HashiCorp Vault (dev mode)
  - external-secrets # External Secrets Operator for automatic secret syncing
  - pihole # Pi-hole DNS server with custom DNS rewrites
