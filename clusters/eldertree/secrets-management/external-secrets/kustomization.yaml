apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  # clustersecretstore.yaml - Applied manually after External Secrets Operator is ready
  # This avoids chicken-and-egg: ClusterSecretStore CRD must exist first
  - externalsecrets/

