apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  # clustersecretstore.yaml - Temporarily disabled until External Secrets Operator CRDs are installed
  # Uncomment after External Secrets Operator HelmRelease is ready
  # - clustersecretstore.yaml
  # externalsecrets/ - Temporarily disabled until ClusterSecretStore is ready
  # - externalsecrets/
