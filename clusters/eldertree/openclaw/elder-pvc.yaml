apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elder-data
  namespace: openclaw
  labels:
    app: openclaw
    component: elder
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
