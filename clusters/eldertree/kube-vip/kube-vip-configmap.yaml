apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-vip
  namespace: kube-system
data:
  vip: "192.168.2.100"
  interface: "wlan0"
  enableControlPlane: "true"
  # Enable LoadBalancer service support (replaces MetalLB)
  enableServices: "true"
  arp: "true"
  leaderElection: "true"
