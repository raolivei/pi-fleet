apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: swimto-redis-scaler
  namespace: swimto
spec:
  scaleTargetRef:
    name: redis
  minReplicaCount: 1
  maxReplicaCount: 3
  pollingInterval: 30
  cooldownPeriod: 300
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "50"
    - type: memory
      metricType: Utilization
      metadata:
        value: "80"

