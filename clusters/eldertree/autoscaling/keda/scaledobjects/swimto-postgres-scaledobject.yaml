apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: swimto-postgres-scaler
  namespace: swimto
spec:
  scaleTargetRef:
    name: postgres
  minReplicaCount: 1
  maxReplicaCount: 2
  pollingInterval: 30
  cooldownPeriod: 300
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "50"
    - type: memory
      metricType: Utilization
      metadata:
        value: "80"

