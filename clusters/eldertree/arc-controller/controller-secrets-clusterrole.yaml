apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: arc-controller-arc-controller-gha-rs-controller-secrets
  labels:
    app.kubernetes.io/component: controller
    app.kubernetes.io/part-of: actions-runner-controller
rules:
  # Allow controller to manage secrets across namespaces
  # Required for reading GitHub PAT and creating JIT runner secrets
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - get
      - list
      - create
      - update
      - patch
      - delete
  # Allow controller to manage roles/rolebindings in runner namespaces
  # Required for setting up and cleaning up listener pod permissions
  - apiGroups:
      - rbac.authorization.k8s.io
    resources:
      - roles
      - rolebindings
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch
  # Allow controller to manage pods in runner namespaces
  # Required for creating ephemeral runner pods
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - get
      - list
      - create
      - delete
      - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: arc-controller-arc-controller-gha-rs-controller-secrets
  labels:
    app.kubernetes.io/component: controller
    app.kubernetes.io/part-of: actions-runner-controller
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: arc-controller-arc-controller-gha-rs-controller-secrets
subjects:
  - kind: ServiceAccount
    name: arc-controller-arc-controller-gha-rs-controller
    namespace: arc-controller
