apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - infrastructure # cert-manager + issuers
  - monitoring # kube-prometheus-stack (Prometheus + Grafana + Alertmanager)
  - observability # Netdata, Grafana Agent, cAdvisor, Kubernetes Dashboard, metrics-server, KEDA
  - canopy # Personal finance dashboard
  - swimto # SwimTO pool schedule app
