{{- if and .Values.nima.enabled .Values.nima.api.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: nima-api-scaler
  namespace: nima
  labels:
    {{- include "keda-scaledobjects.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    name: nima-api
  minReplicaCount: {{ .Values.nima.api.minReplicas }}
  maxReplicaCount: {{ .Values.nima.api.maxReplicas }}
  pollingInterval: {{ .Values.global.pollingInterval }}
  cooldownPeriod: {{ .Values.global.cooldownPeriod }}
  triggers:
{{- if eq (.Values.nima.api.triggerType | default "cpu-memory") "http-rate" }}
    - type: prometheus
      metadata:
        serverAddress: {{ .Values.global.prometheus.serverAddress }}
        metricName: http_requests_per_second
        threshold: {{ .Values.nima.api.httpRateThreshold | default "2" | quote }}
        query: |
          sum(rate(traefik_service_requests_total{
            service="nima-api",
            namespace="nima"
          }[2m]))
{{- else }}
    - type: cpu
      metricType: Utilization
      metadata:
        value: {{ .Values.global.cpuThreshold | quote }}
    - type: memory
      metricType: Utilization
      metadata:
        value: {{ .Values.global.memoryThreshold | quote }}
{{- end }}
{{- end }}

