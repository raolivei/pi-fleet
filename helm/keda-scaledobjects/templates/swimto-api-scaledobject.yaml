{{- if and .Values.swimto.enabled .Values.swimto.api.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: swimto-api-scaler
  namespace: swimto
  labels:
    {{- include "keda-scaledobjects.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    name: swimto-api
  minReplicaCount: {{ .Values.swimto.api.minReplicas }}
  maxReplicaCount: {{ .Values.swimto.api.maxReplicas }}
  pollingInterval: {{ .Values.global.pollingInterval }}
  cooldownPeriod: {{ .Values.global.cooldownPeriod }}
  triggers:
{{- if eq (.Values.swimto.api.triggerType | default "cpu-memory") "http-rate" }}
    - type: prometheus
      metadata:
        serverAddress: {{ .Values.global.prometheus.serverAddress }}
        metricName: http_requests_per_second
        threshold: {{ .Values.swimto.api.httpRateThreshold | default "5" | quote }}
        query: |
          sum(rate(traefik_service_requests_total{
            service="swimto-api-service",
            namespace="swimto"
          }[2m]))
{{- else }}
    - type: cpu
      metricType: Utilization
      metadata:
        value: {{ .Values.global.cpuThreshold | quote }}
    - type: memory
      metricType: Utilization
      metadata:
        value: {{ .Values.global.memoryThreshold | quote }}
{{- end }}
{{- end }}

