{{- if and .Values.canopy.enabled .Values.canopy.redis.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: canopy-redis-scaler
  namespace: canopy
  labels:
    {{- include "keda-scaledobjects.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    name: canopy-redis
  minReplicaCount: {{ .Values.canopy.redis.minReplicas }}
  maxReplicaCount: {{ .Values.canopy.redis.maxReplicas }}
  pollingInterval: {{ .Values.global.pollingInterval }}
  cooldownPeriod: {{ .Values.global.cooldownPeriod }}
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: {{ .Values.global.cpuThreshold | quote }}
    - type: memory
      metricType: Utilization
      metadata:
        value: {{ .Values.global.memoryThreshold | quote }}
{{- end }}

