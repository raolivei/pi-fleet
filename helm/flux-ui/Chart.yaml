apiVersion: v2
name: flux-ui
description: Flux UI (Weave GitOps) for visualizing and managing FluxCD deployments
type: application
version: 0.1.0
appVersion: "2.0.0"
# Note: Weave GitOps will be deployed via HelmRelease referencing HelmRepository
# This chart structure is ready for future dependency management
